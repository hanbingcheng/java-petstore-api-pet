<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.petstore.api.pet.domain.repository.PetRepository">
    <resultMap id="petResultMap" type="com.example.petstore.api.pet.domain.model.PetEntity">
        <id property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="categoryId" column="category_id"/>
        <result property="status" column="status"/>
        <result property="categoryName" column="category_name" />
        <result property="tagIds" column="tag_ids" typeHandler="com.example.petstore.api.common.base.converter.mybatis.Json2ListTypeHandler"/>
        <result property="photoUrls" column="photo_urls" typeHandler="com.example.petstore.api.common.base.converter.mybatis.Json2ListTypeHandler"/>
    </resultMap>

    <select id="findByStatus" resultMap="petResultMap">
        SELECT
            p.*,
            pc.name as category_name
        FROM
            pets p
        INNER JOIN pet_categories pc
            ON p.category_id = pc.id
        WHERE
            p.status = #{status}
        AND
            p.delete_flg = 0
    </select>

    <select id="findByTags" resultMap="petResultMap">
        SELECT
            p.*,
            pc.name as category_name
        FROM
            pets p
        INNER JOIN
            pet_categories pc
        ON
            p.category_id = pc.id
        WHERE
            JSON_CONTAINS(p.tag_ids, JSON_ARRAY(
                <foreach item="tagId" collection="tagIds" separator=",">
                    #{tagId}
                </foreach>
        AND
            p.delete_flg = 0
        ))
    </select>

    <select id="getPetById" resultMap="petResultMap">
        SELECT
            p.*,
            pc.name as category_name
        FROM
            pets p
        LEFT JOIN
            pet_categories pc
        ON
            p.category_id = pc.id
        WHERE
            p.id = #{petId}
        AND
            p.delete_flg = 0
    </select>

    <insert id="insert">
        <selectKey keyProperty="id" resultType="long" order="AFTER">
            SELECT LAST_INSERT_ID()
        </selectKey>
        INSERT INTO pets
        (
            name,
            category_id,
            status,
            tag_ids,
            photo_urls
        )
        VALUES
        (
            #{name},
            #{categoryId},
            #{status},
            #{tagIds, typeHandler=com.example.petstore.api.common.base.converter.mybatis.Json2ListTypeHandler},
            #{photoUrls, typeHandler=com.example.petstore.api.common.base.converter.mybatis.Json2ListTypeHandler}
        )

    </insert>

    <update id="update">
        UPDATE
            pets
        SET
            name = #{name},
            category_id = #{categoryId},
            status = #{status},
            tag_ids = #{tagIds, typeHandler=com.example.petstore.api.common.base.converter.mybatis.Json2ListTypeHandler},
            photo_urls = #{photoUrls, typeHandler=com.example.petstore.api.common.base.converter.mybatis.Json2ListTypeHandler}
        WHERE
            id = #{id}
        AND
            delete_flg = 0
    </update>

    <update id="delete">
        UPDATE
            pets
        SET
            delete_flg = true
        WHERE
            id = #{id}
        AND
            delete_flg = 0
    </update>
</mapper>